# This is a template file. Create secret.yaml with actual values.
# DO NOT commit secret.yaml to git!

apiVersion: v1
kind: Secret
metadata:
  name: portfolio-secrets
  namespace: portfolio-metrics
type: Opaque
stringData:
  # Database password (REQUIRED)
  DB_PASSWORD: "your-database-password-here"
  
  # JWT Secret key (REQUIRED - for authentication token signing)
  # Generate with: openssl rand -hex 32
  SECRET_KEY: "your-secret-key-here"



