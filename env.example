# Project Configuration
PROJECT_NAME=Portfolio Metrics API
PROJECT_VERSION=1.0.0
PROJECT_DESCRIPTION=A FastAPI application for portfolio metrics and analysis

# API Configuration
API_V1_STR=/api/v1

# CORS Settings
ALLOWED_HOSTS=["*"]

# Database Configuration (REQUIRED)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your-database-password-here
DB_SSLMODE=prefer
# For managed services, use: require
# For local dev, use: disable or prefer

# Security Settings (REQUIRED)
SECRET_KEY=your-secret-key-here-change-in-production-generate-a-secure-random-key
ACCESS_TOKEN_EXPIRE_MINUTES=30

# External API Configuration (REQUIRED if using external APIs)
INSTRUMENT_API_NAME=Alpha_Vantage
INSTRUMENT_API_URL=https://www.alphavantage.co/query
INSTRUMENT_API_KEY=your-alpha-vantage-api-key-here

# Environment
ENVIRONMENT=development
DEBUG=true
